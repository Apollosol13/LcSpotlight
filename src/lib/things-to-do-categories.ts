/**
 * Categories merged into the Things To Do filter chips alongside values from the DB.
 * Keeps known buckets visible even when no row uses them yet.
 */
export const THINGS_TO_DO_CATEGORY_PRESETS: readonly string[] = ["Food trucks"];
